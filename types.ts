export interface RadioConfig {
  churchName: string;
  streamUrl: string;
  images: string[];
  primaryColor: string;
}

export interface DailyMessage {
  verse: string;
  reference: string;
  thought: string;
}
